# Config data exposed by config server
info:
  foo: ttsgw-default
  someList: [2, 3, 7]

---
spring:
  profiles: cloud
sftp:
  host: pi.open-rate.com 
  use-password: true
  user: pi
  password: T1gger123
  port: 22
  remote-path: .
  delete-file-after-transfer: true

---
spring:
  profiles: unit-test
info:
  foo: ttsgw-unit-test
