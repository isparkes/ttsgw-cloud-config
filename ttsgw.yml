# Config data exposed by config server
info:
  foo: ttsgw-default
  someList: [2, 3, 7]

---
spring:
  profiles: cloud
sftp:
  host: home.open-rate.com 
  use-password: true
  user: po
  password: T1gg#r123
  port: 22
  remote-path: .
  delete-file-after-transfer: true

---
spring:
  profiles: unit-test
info:
  foo: ttsgw-unit-test
